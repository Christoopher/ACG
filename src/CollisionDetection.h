#ifndef COLLISION_DETECTION_H
#define COLLISION_DETECTION_H

std::vector<Contact> & collision_detection(RigidBody &rigidBodyArray, float t, float dt)
{
	//Egentligen någon typ av broad phase
	//if (collision from broad phase)
	//{
		//Narrow phase (nu brute force'a allt)
		for (int i = 0; i < )
		

	//}

}


#endif